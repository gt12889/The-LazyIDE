# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.json
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Rust / Tauri build artifacts
src-tauri/target/
# Verification artifacts (the .mjs case is covered by the root-anchored
# /_*.mjs rule further down; keep the .png rule explicit and anchored)
/_verify-*.png
# Ad-hoc site screenshots and shot scripts (the _shot-*.mjs script pattern
# is covered by the root-anchored /_*.mjs rule further down)
_site-*/
_site-shots*/
_site-w*/
# Numbered debug screenshots at repo root (00-*.png through 09-*.png)
/0[0-9]-*.png
# Playwright debug screenshots at repo root
/playwright-*.png
# LazyBrain dev brain (local only, not committed)
.lazybrain/
# Agent worktrees (managed by lazy agent pipeline)
.lazy/worktrees/
# Internalized LazyBrain engine (engine/) â€” vendored source lives under
# version control, but its build artifacts do not. Already covered by the
# generic `node_modules` / `dist` rules above; listed explicitly so the
# self-contained engine's ignore policy is documented at the point of intent.
engine/node_modules/
engine/dist/
# Bundled sidecar resources
# node.exe is large (66 MB) â€” downloaded in CI via `npm run bundle:node-exe`, not committed.
# lazybrain/ is vendored into the repo so CI is self-contained; see RELEASING.md.
src-tauri/resources/node.exe
# Un-ignore the vendored lazybrain bundle (overrides the `node_modules` rule above).
# The lazybrain node_modules are intentionally committed so CI needs no external repo.
!src-tauri/resources/lazybrain/
!src-tauri/resources/lazybrain/**
# Secrets â€” never commit
.env
.env.*
!.env.example
!cloud/.env.example
supabase/.env
supabase/.temp/
.tauri/
**/lazy.key
wrangler.toml
r2.env
# Test/coverage artifacts
coverage/
test-results/
playwright-report/
bench/results/
bench/agent-output/
bench/ds-workspace/
bench/.agent-wt/
bench/.swe-wt/
bench/polyglot/.vendor/
# Debug capture screenshots at repo root
/*.png
# Throwaway scripts and driver output relocated out of the repo root
# (see docs/archive/ for the equivalent historical AUDIT.md/CONTINUE-ANALYSIS.html)
/scratch/

# ---------------------------------------------------------------------------
# Root-level ad-hoc scripts, logs, and analysis exports
#
# Manual QA/verification work drops throwaway scripts, PowerShell helpers,
# and debug captures directly at the repo root. All patterns below are
# root-anchored (leading "/") so they never shadow legitimately-named files
# living under scripts/, e2e/, or src/.
# Ad-hoc debug/test scripts at repo root (_final-test.mjs, _qa-driver.mjs,
# _retest.mjs, _reverify.mjs, _v1-mission-test.mjs, _v2-gate-test.mjs, ...)
/_*.mjs
# Ad-hoc Playwright scripts at repo root without an underscore prefix
/playwright-*.mjs
# Any other "*-test.mjs" throwaway script at repo root
/*-test.mjs
# Debug output logs from ad-hoc scripts at repo root (_tauri-dev-err.log,
# _preview.err.log, ...)
/_*.log
# PowerShell crop/screenshot helper scripts at repo root
/*.ps1
# One-off PNG analysis exports at repo root
# (CONTINUE-ANALYSIS.html itself now lives tracked under docs/archive/)
/e2e-desktop-final.png
# One-off vitest console dump at repo root (mo-test.txt, ...)
/mo-*.txt
# Transient Playwright timestamp files (generated by e2e/vite-web.config.ts)
*.timestamp-*.mjs
# Ad-hoc E2E screenshot dumps and driver state files at repo root
# (_e2e-shots/ holds hundreds of throwaway captures; _e2e-state.json,
# _newuser-state.json, _fix9-state.json, _e2e-cmd.json are transient driver
# scratch â€” none belong in a reproducible release state).
/_e2e-shots/
/_*-shots/
/_*.json
# Agent / mission local state
.lazy/
.claude/worktrees/
_mission/
# Subagent-driven-development scratch (ledger, briefs, reports)
.superpowers/
# crash artifacts
*.stackdump
# local dev launcher configs
.claude/launch.json

# Bench workspaces & scratch
bench/humaneval-ws/
bench/aider-polyglot/
/_humaneval.*
/_aider_lb.html


# Benchmark harness (Terminal-Bench 2.1 / DeepSWE) — local only
/_deep-swe/
/jobs/
bench/jobs/
.harbor/
/pier.toml
/harbor.toml


# Lint scratch
/.lint-pid.txt
/lint-errors.txt
# ESLint JSON output dumps (debug-only, paths leak the machine)
/eslint*.json
# Ad-hoc site screenshot script at repo root
/shoot-site.mjs

# UC test harness scratch (session profiles, run captures) — driver.mjs/launch-app.mjs
# restent trackés, seul leur output ne l'est pas.
_uc-tests/.wv2-*/
_uc-tests/runs/
_uc-tests/*.png
_uc-tests/*.out
_uc-tests/dump-after-login.json

# Ad-hoc QA screenshot dumps (racine, hors _e2e-shots/ déjà couvert plus haut)
/e2e-screenshots/
/uc11/
/UsersDavidDocumentscerveauLazy_uc-testsrunsuc10/


_log-*.txt
_tok.txt
_probe*.mjs
_proxy-raw.txt
_matrix.txt
_probe-log.txt
_run-*.bat

# ---------------------------------------------------------------------------
# OSS hygiene — local QA / scrape / runtime (never publish)
# ---------------------------------------------------------------------------
# Ad-hoc QA dumps (scripts, logs, screenshots, design captures)
/_qa/
/_qa-*/
/_qa-*
# Scrape / debug text dumps at repo root
/scrape-result.txt
/scraped-manager.txt
/scraped-*.txt
# Root throwaway JS/TS/TXT used by agents during live QA (not under scripts/)
/_qa-*.js
/_qa-*.ts
/_qa-*.txt
/_qa-*.png
# .lazy/ is already ignored above — local missions, gate-audit, solari
# sessions, and bot state stay on the machine. Documented in OPENSOURCE.md.
# Dist public export (if someone points export:public at ./dist/public-export)
/dist/public-export/

# Accidental Windows path-collapse junk (never publish)
/UsersDavid*

# Old real-app screenshot dump (demo/screenshots) — replaced by the public
# readme captures; not referenced by any code, never publish.
/demo/

# Bundled runtime dependencies are regenerated by bundle:sidecar.
/src-tauri/resources/lazybrain/node_modules/
